{
  "name": "search-task-framework-runner",
  "version": "0.1.0",
  "description": "Concurrent search-task scheduler. Tasks plug in under <repo>/tasks/<name>/.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "setup": "node scripts/setup.js",
    "test": "node --test tests/*.test.js ../../../tasks/*/tests/*.test.js"
  },
  "dependencies": {
    "@mariozechner/pi-ai": "^0.58.4",
    "better-sqlite3": "^12.6.2",
    "dotenv": "^17.2.4",
    "js-yaml": "^4.1.0",
    "web-push": "^3.6.7"
  }
}
